# Copy this file to  agent/.env  and fill in your key. agent/.env is git-ignored.
#
#   cp agent/.env.example agent/.env
#   # then edit agent/.env

OPENAI_API_KEY=sk-your-key-here

# Optional overrides (defaults shown)
OPENAI_MODEL=gpt-4o
