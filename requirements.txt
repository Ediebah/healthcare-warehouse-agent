# App / agent RUNTIME dependencies only — this is what Streamlit Community Cloud installs.
# Pinned to the exact versions the app is verified working on (no dbt: the app never needs it
# at runtime, and dbt's pins were colliding with pandas/numpy/altair on the cloud resolver).
#
# To also build the dbt warehouse (Weekend 1, pipeline_healer, CI), use requirements-dev.txt.
streamlit==1.58.0
openai==2.44.0
python-dotenv==1.2.2
pandas==3.0.3
numpy==2.5.0
altair==6.2.2
duckdb==1.5.4
scipy==1.18.0
statsmodels==0.14.6
patsy==1.0.2
scikit-learn==1.9.0
scikit-survival==0.28.0
plotly==6.8.0
python-docx==1.2.0
vl-convert-python==1.9.0.post1
openpyxl==3.1.5
